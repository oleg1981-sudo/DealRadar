### T-INF-12 [DONE]
Evidence: git status --porcelain | grep -E '^\?\? (docs|tasks|audit)/' returns empty (verified live in /Users/danielmanzela/DealRadar) — no untracked doc/task/audit trees remain. Residual untracked files are only .agents/, .claude/skills/, .firecrawl/, .mcp.json, skills-lock.json, matching the task's documented out-of-scope exception.
Gap: None — acceptance fully met.

### T-DB-0 [DONE]
Evidence: supabase/schema.sql defines record_price_history exactly once (grep 'function record_price_history' → single hit at schema.sql:164, tombstone comment at :274-281 documents the deleted duplicate). Body at schema.sql:164-179 inserts product_id,day,sale_price,original_price,currency with ON CONFLICT (product_id, day) and an IS DISTINCT FROM guard; trigger at :181-184 fires 'after insert or update of sale_price'. .github/workflows/db-migrate.yml:38-41 adds a CI guard: 'Guard — one definition per function/trigger' greps for duplicate 'create or replace function'/'create trigger' names and fails the build (dups=$(grep -oiE ... | sort | uniq -d)).
Gap: None on the on-disk/CI side. The task's own text flags the staging price-changing-upsert drill and prod prosrc/price_history-shape probes as still open (prod DB shape was UNKNOWN as of the doc date) — that live-DB confirmation is outside this worktree's reach and belongs to T-INF-1, not re-verified here.

### T-ING-6 [PARTIAL]
Evidence: M0 restore is DONE and further evolved: scripts/ingest-awin.cjs (388 lines) and .github/workflows/ingest-awin.yml both present with merchant_url (ingest-awin.cjs:164), gallery (:139-146), description (:147-165), fetchExistingPrices verified-price preservation (:262-269, called at :321 before upsert, gallery-richer-wins at :330-332), and stale-hide (:359-361, 'stale-hide: ... deals unseen for 3+ days hidden'). git log shows restore commit 3ed5859 'fix(ingest): restore main's ingest-awin script + workflow reverted by merge 155cf06 (T-ING-6, RSK-14)' followed by 92495dd adding paragraph-preserving descriptions on top. The workflow (ingest-awin.yml:47-78) chains flag-homepage-hidden and snapshot-prices steps post-ingest (DSN-ING-12 flag re-wire). ean_code extraction present at ingest-awin.cjs:169.
Gap: The M1 'live feed' clause (creds set, daily Action upserts >0 real rows, egress ≤~300MB, dry-run writes 0) cannot be verified from code — it depends on real AWIN credentials being live in Netlify/GH secrets (gated on T-ING-2, still unresolved — see below). Also no enforced egress cap exists in code (only a comment '~300 MB / ~200k' at ingest-awin.cjs:3 and a --limit row cap at :36/:312, not a byte-budget abort), so NFR-COST-2's 'alert on a breach' has no monitoring hook.

### T-SEO-1 [PARTIAL]
Evidence: Shared baseUrl helper landed at src/lib/utils/site-url.ts:15-18 (siteUrl(), resolution order NEXT_PUBLIC_APP_URL -> URL -> hardcoded dealradar.me fallback) and is used at all 4+ named call sites: src/app/robots.ts:2,52, src/app/[locale]/deal/[slug]/page.tsx:19,34, src/lib/db/alerts.repo.ts:17,111, src/lib/seo/sitemaps.ts:5,45,62,73, src/app/[locale]/layout.tsx. public/robots.txt is confirmed deleted (ls fails: 'No such file or directory'). robots.ts:37-41 now carries three dedicated AI-bot groups (AI_SEARCH_BOTS incl. OAI-SearchBot/PerplexityBot, AI_TRAINING_BOTS incl. Google-Extended) satisfying the FR-SEO-7 sub-clause.
Gap: The literal acceptance 'grep -rE dealradar.(eu|app) src public = 0' still returns 4 hits (robots.ts:7 comment, site-url.ts:12 comment, alerts.repo.test.ts:103/108/109 — all comments/tests referencing the old bug, not live fallbacks, but literally non-zero). More importantly the required 'CI grep-gate on stale hosts' that fails the build does not exist anywhere: repo has no CI workflow at all (only awin-programmes-sync.yml, db-migrate.yml, ingest-awin.yml, purge-alerts.yml, verify-awin.yml — none run lint/build/grep on PR), package.json has no prebuild/grep script, and scripts/verify-deploy.mjs (which does assert host correctness at lines 24-33,71,85,97) is a manual post-deploy script never invoked from any workflow. Netlify env var value for NEXT_PUBLIC_APP_URL cannot be verified from code (external config).

### T-ING-1 [PARTIAL]
Evidence: A functional mock-pollution gate exists in src/app/api/refresh/route.ts:39-49: 'isReal' filters out any deal whose provider health reports isMock!==false (also hard-excludes dummyjson) BEFORE upsertDeals is called (route.ts:63-65: 'const realDeals = deals.filter((d) => isReal(d.source)); skippedMock += ...; const upserted = await upsertDeals(realDeals)'), so an empty-cred refresh (all providers isMock:true) upserts 0 real rows. Provider isMock flags verified in src/lib/providers/awin.ts:38, kelkoo.ts:46, tradedoubler.ts:33, strackr.ts:33.
Gap: No assertNotMock() function exists anywhere in the repo (grep for 'assertNotMock' across .ts/.js returns zero hits) — the acceptance's named contract ('assertNotMock(deals,{isProd:true}) drops mock rows and throws-to-log') is unimplemented; the gate is an inline filter in refresh/route.ts only, with no second gate inside deals.repo.ts itself (defense-in-depth absent) and no isProd detection. The required test file src/lib/db/deals.repo.test.ts does not exist (find turned up nothing) — the Verify method's specified unit test cannot be run. Prod-side check ('SELECT count(*) FROM deals WHERE source IN (<mock>)' = 0) not independently verifiable from this worktree.

### T-INF-1 [DONE]
Evidence: Live Supabase (MCP list_tables): public.deals(846 rows), price_alerts(9), price_history(7229), transactions(0), affiliate_programmes(21416) all present with rls_enabled=true. mcp__supabase__list_migrations shows 3 applied migrations including the v3.1 baseline (20260710054325 v3_1_remediation_layer_baseline) plus two more landed since (2026-07-14) — proving the runner keeps working. Live execute_sql confirms transactions constraints transactions_product_id_fkey, transactions_commission_chk, transactions_status_chk all present (pg_constraint). Live prosrc for record_price_history matches the fixed single-definition body with `on conflict (product_id, day)`. supabase/schema.sql:121-122 has deals_slug_idx (unique, functional 'public_id' equivalent) and deals_ean_idx; schema.sql:246 enforces `slug not null`.
Gap: pg_policies for the public schema returns ZERO rows across all 5 tables (verified live) — RLS is enabled but no explicit policies exist, so the literal acceptance clause 'pg_policies shows RLS on all tables' (read as policies-present) does not hold, even though the deny-all-by-default posture is not unsafe given the app uses the service-role key server-side (see NFR-SEC-2 finding). price_history table uses `create table if not exists` (schema.sql:129) rather than an explicit guarded old→new ALTER migration as the acceptance text calls for — functionally moot per the task's own 2026-07-10 probe note that prod's price_history was already day-keyed, but the guarded-migration code itself was never written.

### T-INF-2 [DONE]
Evidence: netlify.toml (root) configures `pnpm build` + @netlify/plugin-nextjs. Live curl to https://dealradar.me/ returns 307→/en, /en returns 200 with security headers that byte-for-byte match next.config.mjs's CSP/HSTS/XFO/XCTO/Referrer-Policy/Permissions-Policy strings in the current HEAD (0b78cc3) — strong evidence the live deploy is in sync with current repo config, not a stale build. `curl -s -o /dev/null -w '%{http_code}' 'https://dealradar.me/api/deals?country=DE'` = 200 with real deal JSON (not the old mock/404), confirming the remediated schema (slug-based routing, live DB) is what's actually deployed.
Gap: Could not directly diff a deployed git SHA against branch HEAD (no version/build-info endpoint found) — the DONE verdict rests on strong indirect evidence (config parity + working DB-backed routes) rather than a literal SHA match.

### T-INF-3 [PARTIAL]
Evidence: src/app/api/refresh/route.ts:53-72 implements a bounded worker pool (concurrency env-tunable via REFRESH_CONCURRENCY, default 6) fanning out 16 countries × 10 categories = 160 tasks within one invocation (maxDuration=300). netlify/functions/refresh-deals.mts:29-32 documents the deliberate cron ordering '03:00 ingest → 05:00 verify → 06:00 refresh' as the task's Verify method requires.
Gap: The task's explicit fix-verification fails: `netlify/functions/refresh-deals.mts:2` still reads 'runs every 15 minutes' and `src/app/api/refresh/route.ts:6` still reads 'hits this route every ... 15 minutes' — both false docstrings the task says must be corrected are still present (grep 'grep "15 min" ...' would be non-zero, not the required 0). No measured full-matrix timing evidence exists anywhere in the repo, and there is no chunk-to-≤N-tasks/invocation or GH-Action-offload path — only in-process concurrency, which is not what the acceptance criteria describe as the fallback if the ceiling is insufficient.

### T-INF-4 [NOT_DONE]
Evidence: `.github/workflows/ci.yml` does not exist (`ls` errors 'No such file or directory'; `git log --all --oneline -- .github/workflows/ci.yml` returns nothing — the file never existed in git history). The only workflows present are verify-awin.yml, db-migrate.yml, purge-alerts.yml, awin-programmes-sync.yml, ingest-awin.yml — none of which reference `pnpm build`, `tsc --noEmit`, or `pnpm test` (grep across .github/workflows/*.yml for those strings returns nothing).
Gap: The entire empty-env graceful-degradation CI gate is absent — no clean-env build check, no test-suite gate, exists in CI at all.

### T-INF-11 [NOT_DONE]
Evidence: No `scripts/restore-drill.mjs` exists anywhere in the repo (find returns nothing). No rollback note exists in `supabase/schema.sql` (grep -n 'rollback' returns nothing). `.github/workflows/db-migrate.yml` has no versioning/rollback mechanism — it's a full-file idempotent apply only. Attempted to verify Supabase PITR/backup enablement live but the read was blocked by the session's permission classifier as out-of-scope for this task (direct prod SQL beyond grepping schema.sql was not authorized).
Gap: None of the three code-side deliverables (restore-drill script, documented migration-rollback convention, db-restore fault-class registration) exist in the repo. PITR/backup-enablement itself is a Supabase-dashboard setting outside code — that portion is HUMAN_GATED_OPEN and unverified in this session (permission-denied), but the code side that IS achievable by an agent is simply missing, not merely gated.

### T-ING-2 [HUMAN_GATED_OPEN]
Evidence: This is explicitly a HUMAN-GATED (G1) task per its own Type field — obtaining AWIN/Kelkoo brand approval + live credentials is a contractual/KYC act outside code. Code side is ready to receive it: providers read env at src/lib/providers/awin.ts:31 ('feedConfigured = Boolean(process.env.AWIN_FEED_URL)') and kelkoo.ts/tradedoubler.ts/strackr.ts all flip isMock:false the moment their token env var is set (no code change required); .env.example:21-37 documents the exact variable names (KELKOO_API_TOKEN, AWIN_FEED_URL, TRADEDOUBLER_TOKEN, STRACKR_API_KEY); .github/workflows/ingest-awin.yml:44-46 already wires secrets.AWIN_FEED_URL through to the script.
Gap: No evidence in this codebase confirms real approved credentials are actually set in Netlify/GH Actions env (that lives outside the repo and cannot be probed from here). Until that human/business action happens, initProviders() will keep reporting isMock:true for every provider and the entire M1 revenue slice stays blocked.

### T-ING-3 [HUMAN_GATED_OPEN]
Evidence: Task type is AGENTIC/human-confirmed field-mapping, gated on T-ING-2 creds existing first. Provider files exist at src/lib/providers/{awin,kelkoo,tradedoubler,strackr}.ts with normalize logic ready to receive real payloads.
Gap: The verification harness itself is missing: scripts/verify-provider-mapping.mjs does not exist anywhere in the repo (find/ls scripts/ shows only db-verify.mjs, verify-awin.cjs, verify-deploy.mjs — no verify-provider-mapping.mjs). No provider_first_yield metric exists in code (grep across .ts/.mjs/.cjs = 0 hits), so even once creds land there is no automated 0-yield alert wired. Code side of this task is NOT ready, independent of the human gate.

### T-ING-4 [PARTIAL]
Evidence: Dedup mechanics are implemented and green: src/lib/providers/registry.ts:103-119 dedupeDeals() groups by EAN else slugified name+shop, ties broken by provider priority; `npx vitest run src/lib/providers/registry.test.ts` → 5/5 tests pass (EAN survivor, price-tie→priority, name+shop fallback, case/whitespace normalization, discount-sort+limit). Non-null-slug is structurally guaranteed at write time: deals.repo.ts:27 toRow() computes `generatedSlug = d.slug || slugify(...)-productId` and schema.sql:225-246 backstops with a deals_set_slug BEFORE trigger plus `alter table deals alter column slug set not null`. Discount clamp exists per src/lib/providers/types.ts:122 comment ('Guard: recompute / clamp discount...').
Gap: No per-cycle dedup collapse-ratio logging exists (grep for 'collapse'/'ratio'/'rawDeals' in registry.ts and refresh/route.ts shows no such log line) — the Monitor/Verify clause 'the per-cycle rawDeals→deduped collapse ratio is logged' is unimplemented. The live-data half of the acceptance ('post-refresh count(*) FROM deals WHERE source<>'mock' > 0 for the launch country') cannot be true yet since T-ING-2 has not produced any live credentialed source — no non-mock rows are being written for any launch country today.

### T-ING-5 [PARTIAL]
Evidence: Schema-side prerequisites are in place post-T-DB-0: single record_price_history trigger (schema.sql:164-184) lands exactly one (product_id, current_date) row per sale_price change with currency; update_historical_lows_batch() RPC exists at schema.sql:187-200 and is called from deals.repo.ts's updateHistoricalLows() via .rpc('update_historical_lows_batch'). The second writer, scripts/snapshot-prices.cjs, is now genuinely wired twice daily as its header (snapshot-prices.cjs:3, 'Runs twice a day from the workflows') claims: called from ingest-awin.yml:76-78 (03:00) AND verify-awin.yml:71-73 (05:00) — the stale-comment defect the task flags is fixed now that T-ING-6 restored the ingest-side call.
Gap: The live outcome metrics (an upsert changing sale_price actually landing a row post-migration; historical_low_price reaching ≥60% coverage after ~30 cycles) require live prod cycles and are outside this worktree's reach to probe directly — the task's own text marks the prod-shape probe and staging drill as pending under T-INF-1, which this audit did not re-verify. Also downstream from T-ING-2/T-ING-6 M1: with no live non-mock feed active yet, there are effectively zero real price changes flowing to exercise the trigger at scale.

### T-ING-10 [NOT_DONE]
Evidence: scripts/verify-awin.cjs (336 lines) inspected end-to-end. All four listed defects are still present: (1) incremental flush — patches/okTouch arrays are only applied AFTER the whole Promise.all over all hosts resolves (verify-awin.cjs:315-319: 'for (const p of patches) await applyPatch(...)' happens only once, at the very end, after the full crawl — no ~every-50 flush anywhere in the per-deal loop at :251-290). (2) no --deadline flag exists (grep for 'deadline' across the file and verify-awin.yml returns 0 hits; only a workflow-level `timeout-minutes: 30` at verify-awin.yml:31, which the script itself has no awareness of). (3) exit code is never conditioned on errors/done ratio — the only process.exit(1) calls are for missing env (:61) and an uncaught exception in the final .catch (:320); a 100%-error run still returns exit 0. (4) product_id is never validated/escaped before being embedded in the `in.()` filter — touchLastUpdated (:227-236) builds `"${id}"` directly from DB values and passes the whole joined string through encodeURIComponent as one blob (not per-id), with no `[\w:.-]` regex guard anywhere in the file (grep for 'test('/'match('/'validate'/'sanitiz' shows no such guard for product_id).
Gap: All four acceptance sub-clauses (incremental flush, deadline stop-to-write, non-zero exit >80% errors, product_id escaping) are unimplemented — this task has made zero progress since the doc's own 2026-07-09 'defects open' note; the checkbox is correctly unticked.

### T-ING-11 [PARTIAL]
Evidence: 3 of 4 sub-clauses are DONE: getAllDealSlugs (deals.repo.ts:338-361) filters `.eq('hidden', false)` at :350 — sitemap excludes hidden deals. getRecentlyUpdatedDeals (deals.repo.ts:309-331) filters `.eq('hidden', false)` at :318 — alert reconciliation excludes hidden deals. list/search/dealsByIds already filter hidden at deals.repo.ts:115 and :243. The automated post-ingest flag-homepage-hidden re-sync now exists: .github/workflows/ingest-awin.yml:57-65 runs `node scripts/flag-homepage-hidden.cjs [--apply]` as a workflow step after ingestion.
Gap: The SSR PDP clause is NOT met: getDealBySlug (deals.repo.ts:286-300) also filters `.eq('hidden', false)` at :294 (comment at :292-293 literally says 'a hidden slug must 404, not render publicly'), and src/app/[locale]/deal/[slug]/page.tsx:44 and :65 call `if (!deal) notFound()` — meaning a hidden deal's PDP now hard-404s exactly like an unknown slug, which directly contradicts the acceptance's explicit requirement to 'render the honest expired/OutOfStock state for hidden deals — never InStock, never a live CTA... resolve, don't 404.' There is no second, non-hidden-filtered lookup path used by the PDP to render an OutOfStock state — only one getDealBySlug exists in the repo file (grep for 'export async function getDealBySlug' = 1 hit).

### T-PDP-1 [PARTIAL]
Evidence: SSR renders real deal data and 404s: getDealBySlug(slug) called in generateMetadata + page body, notFound() thrown on null (src/app/[locale]/deal/[slug]/page.tsx:44,65). Rendering strategy IS now declared: `export const dynamic = 'force-dynamic'` (page.tsx:28) — resolves the doc's stale claim that 'today neither is exported'. Query excludes hidden deals (deals.repo.ts:294 `.eq('hidden', false)`).
Gap: The exact DealCard href-mismatch fallback the acceptance names for removal is still present unchanged: `const dealSlug = deal.slug || slugify(deal.productName);` (src/components/deals/DealCard.tsx:28). It was not dropped or resolved into a single non-composing path — the requirement explicitly calls this out by content, not just by line number, and it survives verbatim (in prod every deal from deals.repo.ts fromRow (line 46) already guarantees a non-null `.slug`, so the OR-branch is currently dead in the Supabase path, but the code itself was not fixed as required and NormalizedDeal.slug remains optional in the type — the fragile fallback is unresolved). Also, live LCP<2.5s/CLS≈0 Lighthouse/CrUX verification cannot be established from static code — no field-data evidence available in this pass.

### T-SEO-2 [NOT_DONE]
Evidence: description now correctly falls back to deal.description (src/app/[locale]/deal/[slug]/page.tsx:89), itemCondition/NewCondition, priceSpecification, conditional brand/gtin/mpn all present (page.tsx:90-107).
Gap: The exact defect the task exists to close is unchanged: availability is still the hardcoded literal 'https://schema.org/InStock' at page.tsx:97, never derived from deal/hidden/expired state as the acceptance requires ('never a constant'). Additionally the offer is a single 'Offer' object (page.tsx:93-107), not an 'AggregateOffer' with lowPrice/highPrice/offerCount as FR-SEO-2/T-SEO-2's acceptance literally requires (a deliberate design choice per the comment at page.tsx:77-79, but it means those three fields are structurally absent, not just null). No live Rich Results Test evidence available to confirm 0 errors either way.

### T-SEO-3 [DONE]
Evidence: 90-day-low + verified-at-CET render as plain (non-CSS-hidden) paragraphs at page.tsx:210-213, sourced from page.tsx:111-120; lowText is only set when deal.historicalLowPrice != null and is otherwise null (silently dropped, page.tsx:112-117); verifiedTime uses timeZone: 'Europe/Berlin' (page.tsx:118-120), i.e. CET/CEST not UTC. Copy strings confirmed at src/messages/en.json:44-46 ('Verified at {time} CET.').
Gap: None found.

### T-SEO-4 [DONE]
Evidence: generateMetadata (page.tsx:39-60) emits alternates.canonical (line 56) plus alternates.languages built from routing.locales (line 49-51) with an explicit 'x-default' entry (line 57). src/i18n/routing.ts:4 lists 13 locales (en, de, fr, es, it, pl, nl, pt, sv, ro, da, fi, no), so 13 hreflang + 1 x-default = 14 alternate tags as required. Unknown slugs 404 in metadata resolution (page.tsx:44) before the streaming shell flushes, matching the 'real HTTP 404' note.
Gap: GSC International Targeting = 0 hreflang errors cannot be verified from code (requires live GSC access); hreflang hosts still inherit whatever siteUrl() resolves to (see T-SEO-1 for host-correctness caveats).

### T-MON-1 [PARTIAL]
Evidence: buildSubId/decodeSubId round-trip is lossless and tested green (src/lib/utils/affiliate.ts:41-70; src/lib/utils/affiliate.test.ts:4-32, `npx vitest run` passes 18/18). Per-network param map exists (affiliate.ts:16-21: kelkoo=custom1, awin=clickref, tradedoubler=epi). All 3 real call sites in the current tree pass args in the correct (shopUrl, source, country, category, productId) order: src/app/[locale]/deal/[slug]/page.tsx:69, src/components/deals/DealCard.tsx:26, src/lib/db/alerts.repo.ts:123. The previously-defective dead component `src/components/deals/DealDetailModal.tsx` (which passed productId in the country slot) no longer exists in the repo — deleted in commit 6dee1c9 — so the concrete defect cited in the task is remediated.
Gap: The acceptance criteria explicitly requires either a test that enumerates all decorateAffiliateUrl call sites, OR a refactor to a single options-object argument (to make the arg-order bug class impossible). Neither exists: `grep -rln decorateAffiliateUrl src --include=*.test.*` finds only src/lib/utils/affiliate.test.ts, which tests the function directly with fixed literal args — it never scans/enumerates real call sites in the codebase. The function signature is still 5 positional args (affiliate.ts:73-79), not an options object. So while the bug is currently fixed by deletion, there is no regression guard for a future call site reintroducing the same arg-order mistake — the Verify method ('call-site enumeration test green') is not satisfied.

### T-MON-2 [PARTIAL]
Evidence: Timing-safe dedicated WEBHOOK_SECRET check (no CRON_SECRET fallback) at src/app/api/postbacks/route.ts:28-38, using src/lib/utils/crypto.ts:44-52 timingSafeEqualStr — wrong secret returns 401 (route.ts:36), tested at route.test.ts:93-97. Missing/unconfigured secret fails closed with 503 (route.ts:31-33, tested route.test.ts:99-104). Negative/NaN commission -> 400 at route.ts:82-85 (tested route.test.ts:78-83). Unknown status defaults to 'pending' at route.ts:88-91. Idempotent upsert via `.upsert(row, {onConflict: 'transaction_id'})` at route.ts:130 (tested route.test.ts:70-77, duplicate transaction_id never 500s). FK-null fallback on 23503 implemented and tested (route.ts:133-138; route.test.ts:107-124). All of the above pass `npx vitest run src/app/api/postbacks/route.test.ts` (18/18 green across both suites).
Gap: The core acceptance clause 'tampered/replayed body -> rejected' and the DSN-MON-2/NFR-SEC-4 requirement of an HMAC body signature + replay guard are entirely absent from the implementation. `src/lib/utils/crypto.ts` has no HMAC-signature-verification or nonce/replay-tracking function for postbacks (only `generateUnsubscribeToken`/`verifyUnsubscribeToken` for email, and `timingSafeEqualStr` for the plain secret compare). `route.ts` checkAuth() only compares a static query-string `secret` param — there is no signature header check, no timestamp/nonce replay window, and no test exercising a 'tampered' or 'replayed' postback anywhere in route.test.ts. This is the same gap the requirements doc calls out for NFR-SEC-4 ('HMAC body signature + replay guard beyond the query-param secret') — unimplemented, not merely undertested.

### T-MON-3 [NOT_DONE]
Evidence: The task requires `deals.affiliate_subid` dropped and `subid3` set-outbound iff read-inbound.
Gap: `affiliate_subid` column is still present and actively maintained, not dropped: `supabase/schema.sql:110` (`add column if not exists affiliate_subid text`); still written on upsert at `src/lib/db/deals.repo.ts:39` (`affiliate_subid: d.affiliateSubid ?? null`); still read back at `deals.repo.ts:64`; still referenced by `scripts/db-verify.mjs:84` schema-check list. No provider ever populates `affiliateSubid` (grep across src/lib/providers/ finds zero assignments beyond the type declaration at `src/lib/providers/types.ts:66`), so it round-trips null-to-null — dead plumbing exactly as flagged, but not removed. Separately, `subid3` remains unpaired in the opposite direction from FR-MON-1's dead-modal case: it IS read inbound from postback bodies and persisted (`src/app/api/postbacks/route.ts:20-21,98-99,117`; `schema.sql:253` `subid3 text` column) but it is NEVER set outbound anywhere — `grep -rn subid3 src/` finds only the inbound read/store path in route.ts, no outbound-URL construction sets a tertiary sub-id param in `affiliate.ts`, `DealCard.tsx`, `alerts.repo.ts`, or the PDP page. This is precisely the 'unpaired subid3' anti-pattern FR-MON-4 names as disallowed. `pnpm build` was not re-run as part of this audit but the column/field are demonstrably still present, so the primary acceptance clauses fail regardless.

### T-MON-4 [NOT_DONE]
Evidence: No such script exists.
Gap: `scripts/thin-loop-drill.mjs` does not exist anywhere in the repository (`find . -iname 'thin-loop*'` returns nothing, excluding node_modules/.next). No staging E2E drill script, no evidence of a seed->render->click->postback->assert-row automation. This is a hard NOT_DONE — the north-star proof task has not been built.

### T-CMP-1 [HUMAN_GATED_OPEN]
Evidence: src/app/[locale]/imprint/page.tsx:8-13 still hard-codes the placeholder identity: name 'DealRadar Europe Ltd.', address 'Tech Park Hub, Rue de la Loi 200, 1040 Brussels, Belgium', vat 'BE 0123.456.789'. grep for 'BE 0123' across src/ returns exactly this one hit (non-zero, acceptance requires 0). Email is correctly on @dealradar.me (not .eu). No env-var/CMS injection point exists for counsel-supplied values — the code is not even staged to receive a sign-off, it just has the placeholder inline.
Gap: Task is explicitly G2 human-gated (counsel sign-off) so code alone cannot close it. Code-side readiness is NOT there either: still the literal placeholder company/VAT/address from the doc's own example, with a code comment TODO(legal) acknowledging it's unresolved (page.tsx:4-6). No sign-off record found in the PR/commit history for this content.

### T-CMP-2 [DONE]
Evidence: node scripts/check-i18n.mjs exits 0: 'I18N PARITY: PASS (0 missing, 0 empty)' across 12 non-en locales (154 keys each) and 'I18N USAGE: PASS (0 unresolved keys across 21 scoped files)' — confirming the v3.1-added code-usage scan phase (scripts/check-i18n.mjs:44-79) is present and passing. Footer routes to localized pages: src/components/layout/Footer.tsx:16-18 links to `/${locale}/imprint`, `/${locale}/privacy`, `/${locale}/terms`. The specific defect cited in the requirement (deal.priceNote missing from all locales, rendering as a literal key on DealCard) is fixed: src/messages/en.json:47 has 'priceNote', and DealCard.tsx:107 calls t('priceNote') successfully (confirmed present in all 12 non-en + en = 13 locale files via the passing parity check).
Gap: None found against the stated acceptance criteria.

### T-CMP-3 [PARTIAL]
Evidence: The badge itself is correctly wired and visible (not sr-only, not footer-only): src/components/deals/SponsoredBadge.tsx renders a Badge with visible text (variant='sponsored'); used at DealCard.tsx:88 and deal/[slug]/page.tsx:229 (comment there: 'Proof fields (visible, not sr-only)', page.tsx:208). The latent counterexample flagged in the spec (DealDetailModal.tsx with a badge-less monetized CTA) no longer exists — `find src -iname '*DealDetailModal*'` returns nothing, i.e. the dead code was deleted rather than wired-and-left-badgeless. All 4 current decorateAffiliateUrl call sites (deal/[slug]/page.tsx, DealCard.tsx, affiliate.test.ts, alerts.repo.ts) were enumerated.
Gap: No test exists enforcing badge presence. `find src -iname 'DealCard.test*'` returns nothing (the Files list in the task names src/components/deals/DealCard.test.tsx, which does not exist), and no other test/e2e file references 'Sponsored' or 'badge' in the deals or PDP area. The task's core acceptance clause — 'a test asserts... enumerates ALL components calling decorateAffiliateUrl' — is unmet even though the underlying UI wiring is correct.

### T-CMP-4 [DONE]
Evidence: src/components/consent/CookieConsent.tsx:26-39 configures vanilla-cookieconsent with `analytics: { enabled: false }` (opt-in, default OFF) and equal-weight buttons (`equalWeightButtons: true, flipButtons: false` at lines 23-24) for both the consent modal and preferences modal. Footer control re-opens it: Footer.tsx:4,19 renders `<CookieSettingsButton>` which calls `openCookiePreferences()` (CookieConsent.showPreferences(), CookieConsent.tsx:73-75). Analytics loading is fully consent-gated: src/components/analytics/Analytics.tsx:114-131 only calls loadClarity()/loadGa() when `CookieConsent.acceptedCategory('analytics')` is true, with autoClear on revoke (CookieConsent.tsx:34-37) erasing _clck/_clsk/_ga cookies. The only cookie set unconditionally is the necessary/functional `dr_location` cookie (middleware.ts:24, useLocation.tsx:32) which the code's own category model classifies as 'necessary: always on (language + location)' (CookieConsent.tsx:6-7).
Gap: No automated test or live DevTools capture was run in this audit (would require a browser session) — this verdict is based on static code review of the consent-gating logic, which is structurally sound. The dr_location necessary-cookie classification is a defensible-but-debatable interpretation of 'strictly necessary' under GDPR that a regulator could contest; flagged as residual risk, not a code defect against the stated acceptance.

### T-CMP-5 [PARTIAL]
Evidence: HMAC verify/delete/idempotency all correct: src/lib/utils/crypto.ts:31-45 timingSafeEqual verification that fails closed (throws in prod if CRON_SECRET absent, crypto.ts:13-14); unsubscribe route.ts:10-21 deletes idempotently ('idempotent: success even if the row was already gone'). List-Unsubscribe/-Post headers present and tested: alerts.repo.ts:129-130, asserted in alerts.repo.test.ts:103-111. Daily purge wired: purge-alerts.yml cron '0 5 * * *' calling POST /api/purge-alerts (protected by CRON_SECRET, route.ts:15-20), which runs supabase/schema.sql:286-300 purge_stale_price_alerts (365d retention / 30d notified). The dealradar.eu-fallback defect cited in the acceptance is FIXED: alerts.repo.ts:111,120 uses `siteUrl()` (src/lib/utils/site-url.ts:16), whose fallback is 'https://dealradar.me', not '.eu' — and this is explicitly regression-tested (alerts.repo.test.ts:103 'never the dead dealradar.eu fallback').
Gap: The specific regression named in the v3.1 acceptance is NOT fixed: 'the GET verb renders a one-button confirmation page whose button POSTs' — it does not. unsubscribe/route.ts:43-55 GET handler still calls unsubscribe() directly and deletes the row on GET, exactly the state-changing-GET defect the spec describes (a mail-scanner prefetch would still silently unsubscribe a user). No test file exists for the unsubscribe route (`find src -iname '*unsubscribe*test*'` empty; `find src/app/api -iname '*.test.*'` only shows postbacks/route.test.ts) — so the required 'both-verb unsubscribe tests + idempotency' and 'GET without confirming POST deletes nothing' verifications have not been run/encoded anywhere.

### T-CMP-7 [PARTIAL]
Evidence: Core dispatch logic works and is unit-tested: alerts.repo.test.ts:82-100 confirms notifyPriceDrops emails exactly once for a qualifying drop and 0 times when price is not below target (strict threshold). The hidden-deal leak named in the spec is FIXED in both alert-sourcing paths: deals.repo.ts:318 `getRecentlyUpdatedDeals` now has `.eq('hidden', false)` (previously missing per the spec's own citation), and deals.repo.ts:243 `dealsByIds` also filters `.eq('hidden', false)` (used by notifyPendingAlerts, alerts.repo.ts:160). List-Unsubscribe header dispatch confirmed (alerts.repo.ts:129-130, tested). The dead `src/lib/email/unsubscribe.ts` file named for deletion is already gone (`ls` confirms no such file; no remaining imports). The IP rate-limiter's fail-open-without-Upstash behavior IS documented in code: redis.ts:49-51 'Fails OPEN when Upstash is unconfigured (local dev) with a one-time warn' plus inline comments at lines 60-66 and 79-80.
Gap: Three required sub-clauses are unmet: (1) the per-email alert cap is still a check-then-insert race, NOT DB-enforced — alerts/route.ts:47-51 does `countActiveAlerts()` then `createPriceAlert()` as two separate calls with no DB trigger/guarded-RPC (grep for a trigger tied to price_alerts cap in schema.sql found none), and nowhere is this race documented as an accepted risk (no comment near countActiveAlerts/createPriceAlert in alerts.repo.ts addressing it) — neither the 'DB-enforced' nor the 'documented overshoot' alternative from the acceptance is satisfied. (2) double-opt-in is not recorded anywhere as a deferred product decision (`grep -ri double-opt-in` across src/ returns zero hits). (3) The design.md-cited 'post-ingest /api/refresh-alerts step' in .github/workflows/ingest-awin.yml does not exist (grep for 'refresh-alerts'/'api/refresh' in that workflow file: zero hits) — the only path that reconciles AWIN-ingested price drops is the daily Netlify 06:00 /api/refresh function calling notifyPendingAlerts(), which functionally covers the gap but does not match the seam the design doc claims. No integration test exists directly against src/app/api/refresh-alerts/route.ts, and no test specifically exercises the hidden-deal-produces-0-dispatch scenario (it follows from the code fix but is untested).

### T-INF-5 [DONE]
Evidence: next.config.mjs:10-33 sets Content-Security-Policy, Strict-Transport-Security (max-age=63072000; includeSubDomains; preload), X-Content-Type-Options: nosniff, X-Frame-Options: DENY, Referrer-Policy, Permissions-Policy, X-DNS-Prefetch-Control on every route. Live `curl -sI https://dealradar.me/en` confirms all of these headers present verbatim in production, byte-identical to the source.
Gap: Could not literally query securityheaders.com's grade from this sandbox (no external grading tool available), but the header set present (HSTS w/ preload, CSP, XFO, XCTO, Referrer-Policy, Permissions-Policy) is comprehensive and consistent with an A-grade result.

### T-INF-6 [PARTIAL]
Evidence: src/middleware.ts:11-29 reads x-nf-country / x-vercel-ip-country / cf-ipcountry, defaults to 'DE' when missing or unsupported. Live-verified: `curl -H 'x-nf-country: FR' https://dealradar.me/` → `dr_location=FR|`; `curl -H 'x-nf-country: XX' https://dealradar.me/` → `dr_location=DE|` (both confirmed against production). No lat/long column exists in supabase/schema.sql (grep for lat/lon/latitude/longitude returns nothing). A city-sanitizer does exist: src/lib/db/deals.repo.ts:270 `sanitizeCity()` strips to `[\p{L}\p{N} .'-]` and caps at 80 chars before interpolating into a PostgREST `.or()` filter, closing the actual injection surface.
Gap: src/middleware.ts itself never reads or sets a city value at all (dr_location is always `COUNTRY|` with an empty city) — the edge-geo sanitization described by the task doesn't live at the edge. The client-side geo pipeline that DOES populate city (src/app/api/geo/route.ts, src/lib/geo/resolve.ts:40) stores third-party API city text into a cookie with zero sanitization; sanitizeCity only runs later, at DB-query time. No unit test exists anywhere for city-injection or for sanitizeCity (no src/middleware.test.ts, and `grep -rln sanitizeCity src/ --include=*.test.ts` returns nothing) — the task's explicit Verify method ('a city-injection unit test') is unmet.

### T-INF-7 [NOT_DONE]
Evidence: src/lib/cache/redis.ts:50-84 implements exactly one rate limiter, `rateLimitAlerts()` (5 requests/hour), used only by src/app/api/alerts/route.ts (confirmed via grep — it's the only match for rateLimit*/Ratelimit under src/app/api/). Grepping each of the four required endpoints (src/app/api/refresh/route.ts, src/app/api/postbacks/route.ts, src/app/api/search/route.ts, src/app/api/deals/route.ts) for rateLimit/429/Ratelimit returns nothing. Live test: 8 consecutive `curl` calls to `https://dealradar.me/api/deals?country=DE` all returned 200, never 429.
Gap: None of the four named endpoints (/api/refresh, /api/postbacks, /api/search, /api/deals) have any rate limiting at all — only the unrelated /api/alerts endpoint (not in the task's target list) has a limiter. No 'rate-limiter unconfigured' warning exists for these four routes either.

### T-SEO-5 [HUMAN_GATED_OPEN]
Evidence: Code side is ready: sitemap is no longer a static/build-frozen prerender — src/app/sitemap.xml/route.ts:5 and src/app/sitemaps/[file]/route.ts:4 both set `export const revalidate = 3600` (hourly ISR), and getAllDealSlugs (src/lib/db/deals.repo.ts:338-363) is DB-driven, hidden-filtered (.eq('hidden', false), line 350) and paginates past the PostgREST 1000-row cap via REST_PAGE=1000 range-looping (lines 346-361), so a deal ingested after the last deploy appears within the hour without a redeploy.
Gap: Submitting the sitemap to Google Search Console and confirming '>= 1 PDP moves to Indexed' requires a human action inside the GSC dashboard with real prod credentials, which no code in this repo can perform or verify; no evidence in-repo of that submission having occurred.

### T-SEO-6 [PARTIAL]
Evidence: Several DSN-SEO-6/7 sub-components have landed since the doc's authoring: sitemap sharding+ISR+hidden-exclusion+pagination (src/lib/seo/sitemaps.ts, deals.repo.ts:338-363, see T-SEO-5); '/search' now emits noindex via generateMetadata (src/app/[locale]/search/page.tsx:16-17), so the 'DSN-SEO-7 /search noindex NOT built' claim in the doc is stale-in-the-good-direction.
Gap: The core public_id-stable URL contract (FR-SEO-1's regex ^/{locale}/deal/[a-z0-9-]+-d[0-9a-f]{12}$) is absent: slugs are still composed as slugify(name)+productId with no 12-hex public_id suffix (deals.repo.ts:27,46; DealCard.tsx:28 `deal.slug || slugify(deal.productName)`), so D11 ('writers stop composing slugs') is still actively violated at the same class of call sites the doc names. No 308-redirect-on-rename mechanism exists anywhere (grep for 308/permanentRedirect in the PDP route and middleware.ts returns nothing). Hidden deals 404 (deals.repo.ts:292-294, 'a hidden slug must 404, not render publicly') rather than serving the required 200+OutOfStock+disabled-CTA page — an explicit, intentional divergence from the FR-SEO-1 design note. category/[slug]/page.tsx has no generateMetadata export at all (grep confirms none), so category canonical/hreflang (FR-SEO-8) is still missing. scripts/smoke-spine.mjs (49 lines) still contains no 308/publicId/expired/hidden assertions.

### T-PDP-2 [NOT_DONE]
Evidence: Category route is src/app/[locale]/category/[slug]/page.tsx (single dynamic segment), gating only on the fixed top-level `CATEGORY_SLUGS` enum of 10 entries (src/lib/providers/types.ts:13-16) — not a `[...slug]` catch-all and not the ~300-leaf taxonomy. `DealFilters` in this route has no subcategory/leaf param at all.
Gap: src/lib/categories.ts:16-17 explicitly documents the anti-pattern the FR forbids: leaf names 'are display strings mapped to search terms (`/search?category=<slug>&q=<name>`), not i18n keys'. The PDP's own subcategory breadcrumb link also builds a `/search?category=...&q=...` URL (page.tsx:133). No `generateStaticParams` for leaf terms exists anywhere; crawlable non-deal category URL count is still ~10 (top-level only), not ≫10 as required.

### T-PDP-3 [NOT_DONE]
Evidence: n/a — nothing implemented.
Gap: Repo-wide grep for `deal_curation`, `legal_ok`, `curation`, `editorial` across src/, supabase/, scripts/ returns zero hits. supabase/schema.sql has no `deal_curation` table. No SSR LEFT JOIN exists in deals.repo.ts or the PDP page for any curation overlay. This task has no code scaffolding at all — task doc's un-ticked status is accurate and current.

### T-PDP-4 [NOT_DONE]
Evidence: n/a — nothing implemented.
Gap: `scripts/author-curation.mjs` does not exist (find/ls returns nothing). No `specs`/`comparison`/`faq` blocks render anywhere on the PDP (src/app/[locale]/deal/[slug]/page.tsx only renders a real-identifiers `specsTitle` dl of brand/model/EAN at lines 259-283, which is NOT the editorial specs/multi-merchant-comparison/FAQ content this task requires). Depends entirely on the nonexistent T-PDP-3 `deal_curation` table/legal_ok gate, which also doesn't exist.

### T-PDP-5 [DONE]
Evidence: src/lib/utils/product-details.ts is now 38 lines total, containing only `unproxyImage()` and `productGallery()` — grep for `productSizes|productSpecs|otherStoreOffers` across src/ returns 0 hits repo-wide (confirmed empty). `productGallery` has a live consumer: imported and called at src/app/[locale]/deal/[slug]/page.tsx:16,80, feeding the real `DealGallery` component (page.tsx:184-192) and JSON-LD `image` (page.tsx:86). `DealDetailModal.tsx` no longer exists anywhere in the tree (`find` across the whole repo returns nothing; git log confirms it existed historically but is gone from HEAD) — so there is no dead-code-importing-dead-code chain left to break. ESLint run clean on product-details.ts and its PDP consumer.
Gap: None found against the (updated v3.1) acceptance text. Note the task doc's own prose ('Keep unticked... DealDetailModal imports productSizes/productGallery') is now stale/regressed-fixed — the modal has since been deleted entirely rather than merely fixed, which independently satisfies the acceptance.

### T-PDP-6 [PARTIAL]
Evidence: Gallery: real feed gallery renders via <DealGallery> fed by productGallery(deal) (page.tsx:80,184-192). Description: <DealDescription html={deal.descriptionHtml} text={deal.description}> renders merchant HTML or feed text (page.tsx:256); JSON-LD `description` uses `deal.description || fallback` (page.tsx:89), satisfying FR-SEO-2's description clause. Modal wire-or-delete: DealDetailModal.tsx has been deleted from the repo entirely (confirmed absent via find; git log shows prior existence only) — satisfies the 'OR the file is deleted' branch, and trivially removes the T-MON-1 arg-order bug and missing-badge issue that would have applied if wired. price_history: src/lib/db/price-history.repo.ts queries and returns day-ordered (oldest→newest) points (query orders `day desc` then `.reverse()`, lines 25-31); /api/price-history/route.ts exists, validates productId, Redis-caches, returns `{points, cached}`.
Gap: The 90-day cardiogram is NOT actually reachable end-to-end: PriceHeatBar's `series` prop is never passed by either call site. PDP page.tsx:217-223 calls `<PriceHeatBar window={pw} .../>` with no `series=`; DealCard.tsx:79-85 likewise passes only `window`. The chronological-series builder `priceSeries()` (src/lib/utils/price-history.ts:47-54) that would translate the fetched `history` into that prop is defined but has ZERO importers anywhere in src/ (grep confirms). Consequently PriceHeatBar always renders in its two-point straight-line fallback mode (PriceHeatBar.tsx:40 `chronological = series !== undefined && series.length >= 2` is always false), never the real recorded day-by-day curve — this is exactly the DSN-PDP-7 gap the design doc calls out ('PriceHeatBar's series prop is unreachable from product code') and it remains true post-implementation. The PDP does fetch `history` via `queryPriceHistory` and folds it into `priceWindow()`'s low/high span (page.tsx:73-74), so the recorded data influences the range shown, but not the plotted curve shape.

### T-SEO-11 [NOT_DONE]
Evidence: The exact crawl-trap named in the spec is unchanged in current code: src/components/home/HeroDeals.tsx:72 still has `href="/search?sort=random"` on the 'View all deals' CTA. src/app/[locale]/search/page.tsx:50 still does `sort === 'random' ? Math.floor(toNum(one(sp.seed)) ?? randomSeed()) : undefined` — an uncached, force-dynamic route (line 11: `export const dynamic = 'force-dynamic'`) that mints a fresh Math.random-derived seed per request when none is supplied, then bakes it into every pagination href via `linkParams.seed = String(seed)` (line 75) consumed by the otherwise-conformant Pagination component (src/components/search/Pagination.tsx). The identical pattern is replicated verbatim in category/[slug]/page.tsx:43,56,75.
Gap: None of the three required fixes landed: seed is not stabilized/removed from crawlable hrefs on either search or category pages, and the HeroDeals CTA still targets the seeded URL rather than a stable one. Only the numbered-Pagination component itself (real <a> hrefs, clean page-1 URL, aria-current) is conformant, matching the doc's separately-tracked FR-SEO-15 'DONE merged-on-branch' claim, which does still hold.

### T-SEO-7 [NOT_DONE]
Evidence: No RelatedDeals.tsx component exists anywhere in src (find/grep both return empty), and the PDP renders only 3 internal <Link> elements total — all breadcrumb (home, category, optional subcategory) at page.tsx:159,165,173. There is no related-deals or same-category deal grid on the PDP.
Gap: Average internal outlinks per PDP remains ~3 (breadcrumb only), far short of the required >=8; the expired/hidden-state page (which doesn't exist — see T-SEO-6) can't link to live alternatives either.

### T-SEO-8 [NOT_DONE]
Evidence: No scripts/poll-index-coverage.mjs and no .github/workflows/index-poll.yml exist anywhere in the repo (find returns nothing for either). No 'pollCoverage' symbol and no GSC/Search Console API integration exists in src or scripts (grep -i for pollCoverage/searchconsole/webmasters across src, scripts, .github returns nothing).
Gap: A separate, adjacent capability (scripts/indexnow-submit.cjs, wired into .github/workflows/ingest-awin.yml and verify-awin.yml) pings IndexNow with changed deal URLs — real value, but it is push-submission only, not GSC coverage polling, and never writes a {indexed:int} signal anywhere; the task's core acceptance (pollCoverage() writing an indexed count) is entirely unimplemented.

### T-SEO-9 [NOT_DONE]
Evidence: No scripts/probe-citations.mjs and no .github/workflows/citation-probe.yml exist in the repo (find returns nothing for either pattern).
Gap: No AI-engine citation probing capability of any kind exists; nothing to verify against ChatGPT/Perplexity/Gemini/AI-Overviews.

### T-SEO-10 [NOT_DONE]
Evidence: No operator_signals table exists in supabase/schema.sql (grep for 'operator_signals' and 'create table' shows only deals, price_alerts, price_history, transactions, affiliate_programmes). No GSC Search Analytics API call exists anywhere in src or scripts (grep -i for searchconsole/search_analytics/webmasters returns nothing).
Gap: This task is entirely unbuilt; it also has no independent execution path since its prerequisite T-SEO-8 (the shared GH-Action cron) doesn't exist either.

### T-CMP-6 [NOT_DONE]
Evidence: No implementation exists anywhere in the current codebase. `find src/app/api -iname '*consent*'` returns nothing (no api/consent/route.ts). `grep -rl consent_audit` across the whole repo (excluding node_modules/.next) returns zero matches — no such table in supabase/schema.sql. The CookieConsent.tsx component has no event handler wired to any server call on consent choice (no fetch/POST on 'cc:onConsent'/'cc:onFirstConsent' in src/components/consent/).
Gap: The entire feature — a server-side consent_audit table and the /api/consent write path that FR-CMP-7/NFR-PRIV-3 require — is absent. This is not a partial gap; nothing has been built for this task despite the design.md DSN-CMP-7 seam citing `api/consent/route.ts` and `consent_audit` as if they exist.

### T-INF-8 [PARTIAL]
Evidence: supabase/schema.sql:121-122 has deals_slug_idx (unique index on slug — the functional 'public_id' hot path) and deals_ean_idx (on ean_code where not null); lines 39-52 add deals_country_discount_idx, deals_country_category_discount_idx, deals_country_brand_idx, deals_name_trgm_idx, deals_brand_trgm_idx covering the listing/search hot paths.
Gap: No `scripts/explain-gate.mjs` exists anywhere in the repo (find returns nothing) — there is no EXPLAIN CI gate at all, which is the task's core verify deliverable. The exact acceptance-named indexes `deals_public_id_idx` and `deals_active_fresh_idx` do not exist under those literal names/shapes (deals_slug_idx is the closest functional equivalent for public_id; nothing indexes on hidden+active+freshness together).

### T-ING-7 [PARTIAL]
Evidence: Config-side expansion is complete and matches the target counts: src/lib/providers/types.ts:7-9 SUPPORTED_COUNTRIES lists 16 countries (DE,AT,FR,ES,IT,PL,NL,PT,SE,RO,GB,BE,DK,FI,NO,CH); src/i18n/routing.ts:4 LOCALES lists all 13 locales (en,de,fr,es,it,pl,nl,pt,sv,ro,da,fi,no). refresh/route.ts:31 uses the full `[...SUPPORTED_COUNTRIES]` by default (not a restricted subset) for the fan-out.
Gap: The acceptance's live-data clause ('≥4 countries return non-mock deals') cannot be true today: with no live provider credentials active (T-ING-2 still HUMAN_GATED_OPEN, all providers isMock:true), zero countries currently return non-mock deals — the fan-out is fully configured but has nothing real to fan out. Whether the full-matrix (16×10=160 tasks) actually stays within the verified serverless ceiling with no timeout (T-INF-3's concern) was not independently re-timed in this audit.

### T-ING-8 [NOT_DONE]
Evidence: No cross-network EAN-collapse mechanism exists between AWIN feed-ingested rows (written directly to Supabase by scripts/ingest-awin.cjs, bypassing the app entirely) and per-query provider rows (written via src/lib/db/deals.repo.ts upsertDeals). upsertDeals (deals.repo.ts:68-77) upserts strictly `onConflict: 'product_id'` with no EAN lookup/reconciliation against existing rows. The in-memory dedupeDeals() (registry.ts:103-119) only dedupes within a single fetchDealsAcrossProviders() call's raw batch (per-query providers only — AWIN's query-path provider returns [] per registry.ts comments, since it's feed-ingested out of band) — it never sees AWIN's already-persisted rows. scripts/ingest-awin.cjs itself has no EAN cross-check against non-AWIN deals.source rows (grep for 'ean' in ingest-awin.cjs only shows the column extraction at :169, no dedup query).
Gap: An AWIN row and a Kelkoo row sharing the same EAN will persist as two separate rows today (different product_id prefixes, e.g. 'awin:X' vs 'kelkoo:Y', both survive the onConflict:'product_id' upsert) — the acceptance's core requirement ('collapse to one survivor') is unimplemented. No SQL reports EAN-key vs name-key collapse counts anywhere.

### T-ING-9 [NOT_DONE]
Evidence: No merchant_commission table or commission-rate model exists in supabase/schema.sql (grep 'commission' only turns up the postback ledger's `transactions.commission_earned` column at schema.sql:148 and its check constraint at :263-266 — an outcome-tracking field, not an input rate feed). dedupeDeals (registry.ts:103-119) still selects the survivor purely by `deal.salePrice < existing.salePrice`, confirmed by its own test title at registry.test.ts:30 ('groups by EAN and keeps the lowest sale price') and the passing test run — min-price-wins, not max(rate×price)-wins.
Gap: None of the acceptance's mechanics exist: no rate-feed ingestion, no expected-commission comparison, no price-only degrade-with-signal path when the rate feed is null. This is a straightforward NOT_DONE — the task is explicitly blocked on A-17 (external rate-feed assumption) per its own Type note, and no scaffolding has been built ahead of that dependency.

### T-MON-5 [NOT_DONE]
Evidence: No reconciliation job, workflow, or DB table exists.
Gap: `scripts/reconcile.mjs` does not exist (`find . -iname 'reconcile*'` returns nothing outside node_modules/.next). `.github/workflows/reconcile.yml` does not exist (`.github/workflows/` only contains awin-programmes-sync.yml, db-migrate.yml, ingest-awin.yml, purge-alerts.yml, verify-awin.yml). No `reconciliation` table/columns exist in `supabase/schema.sql` (`grep -n reconciliation supabase/schema.sql` returns nothing). `git log --all --grep` for reconcile/payout across the whole history finds zero commits touching either concept. This is unbuilt, not merely unverified.

### T-MON-6 [NOT_DONE]
Evidence: The disbursement-avoidance half of the acceptance criteria IS met: grep across src/, supabase/, scripts/ for 'disburs|payout|bank' finds zero actual code paths (only an unrelated GDPR-copy string mentioning 'Datenbank-Hosting' in a German locale file) — no disbursement/banking code has shipped, correctly respecting the G3 gate per A-15.
Gap: The read-only `payout_ready` view itself — explicitly called out in the task as the deterministic, non-gated part of the work ('the read-only view itself is deterministic... No disbursement code ships pre-entity') — does not exist. `grep -n 'create.*view' supabase/schema.sql` returns zero matches; there are no views defined in schema.sql at all. Since building this view requires no human/business action (only the disbursement/banking side is G3-gated), its absence is a plain NOT_DONE on the code-buildable portion, not a legitimate human-gate block. The G3 gate remains correctly unopened (no disbursement code), but the task's actual code deliverable has not shipped.

### T-OPS-1 [NOT_DONE]
Evidence: supabase/schema.sql (335 lines total) contains only 5 tables: deals, price_alerts, price_history, transactions, affiliate_programmes (grep for 'operator_signals'/'operator_incidents' returns zero matches across the whole repo). No src/lib/observability/ directory exists (`ls src/lib` lists only: analytics, cache, categories-i18n.ts, categories.ts, db, email, geo, i18n, product-details-i18n.ts, providers, seo, utils). netlify/functions/ contains only refresh-deals.mts — no uptime-probe.mts. No error-tracking dependency (Sentry etc.) in package.json.
Gap: None of the acceptance criteria are met: no operator_signals/operator_incidents tables, no emit.ts, no uptime-probe.mts, no error tracker wired in anywhere. `git log --all` shows this was never built (grep for operator/observability/deadman/game-day/uptime across all commits returns no relevant hits). This is the declared 'prerequisite for all of S6' and it does not exist, so T-OPS-2 through T-OPS-6 are structurally blocked.

### T-INF-9 [NOT_DONE]
Evidence: `find . -iname '*check-budget*'` and `find . -iname '*cost-guardrail*'` (excluding node_modules) both return zero results. `scripts/check-budgets.mjs` does not exist; `.github/workflows/cost-guardrail.yml` does not exist.
Gap: No cost-guardrail mechanism of any kind is implemented — no DB-row-count check, no GH-Action-minutes check, no AWIN-egress check, no Vertex/LLM token-spend check, and no max-iteration guard on any agentic cron.

### T-INF-10 [NOT_DONE]
Evidence: grep for 'cron_heartbeat' across all .ts/.mts/.yml/.mjs files in the repo returns zero matches. No `src/lib/observability/emit.ts` (or any observability module) exists. Worse: netlify/functions/refresh-deals.mts:11-14 does the OPPOSITE of the requirement on a missing secret — `console.warn('[refresh-deals] URL or CRON_SECRET not set — skipping run'); return new Response('not configured', { status: 200 })` is a silent success-status exit, not an alarm. .github/workflows/db-migrate.yml similarly does `echo "SUPABASE_DB_URL secret not set — skipping migration"; exit 0` on a missing secret.
Gap: No heartbeat signal is ever written by any scheduled job, and the two places that check for a missing secret both exit successfully/silently instead of alarming — the exact anti-pattern the task was written to eliminate is still present in both refresh-deals.mts and db-migrate.yml.

### T-OPS-2 [NOT_DONE]
Evidence: No src/lib/operator/ directory exists anywhere in the repo (find for '*operator*' excluding node_modules/worktrees returns zero results). No netlify/functions/operator-tick.mts. `runMonitors()` is not defined anywhere in the codebase (grep for 'runMonitors' across src, netlify, scripts returns nothing).
Gap: The entire deterministic monitor suite is absent — no threshold checks for mock-rows, upserted=0, staleness, 404-spike, JSON-LD drop, index stall, postback 401/FK-null, commission=0, cost breach, or cron silence. This task also structurally depends on the operator_signals substrate from T-OPS-1, which itself does not exist.

### T-OPS-3 [NOT_DONE]
Evidence: No src/lib/operator/playbooks.ts exists; the file-search for '*operator*' across the whole repo (excluding node_modules and nested worktree) returned zero matches.
Gap: No `heal(faultClass)` function or any self-heal logic exists anywhere in the codebase. There is no re-run-refresh/purge-mock-rows/resubmit-sitemap/re-render playbook runner, and no escalate-after-2-failures logic.

### T-OPS-4 [NOT_DONE]
Evidence: scripts/ directory listing shows: apply-schema.mjs, awin-programmes-sync.cjs, build-mock-images.cjs, check-i18n.mjs, db-verify.mjs, enrich-galleries.cjs, flag-homepage-hidden.cjs, hero-illustration.source.png, indexnow-submit.cjs, ingest-awin.cjs, lib, smoke-spine.mjs, snapshot-prices.cjs, strip-labels.cjs, verify-awin.cjs, verify-deploy.mjs — no operator-triage.mjs. .github/workflows/ contains only awin-programmes-sync.yml, db-migrate.yml, ingest-awin.yml, purge-alerts.yml, verify-awin.yml — no operator-triage.yml.
Gap: No agentic triage composer exists at all; no `triage(signalBundle)` function found anywhere in the repo. This depends on the (nonexistent) operator_signals substrate as its input.

### T-OPS-5 [NOT_DONE]
Evidence: No src/lib/operator/escalate.ts and no netlify/functions/deadman-watchdog.mts exist; netlify/functions/ contains only refresh-deals.mts (confirmed via directory listing).
Gap: No `escalate(summary)` delivery function, no ack mechanism, and no independent dead-man's-switch cron exist in the codebase.

### T-OPS-6 [NOT_DONE]
Evidence: No scripts/game-day.mjs and no .github/workflows/game-day.yml exist (confirmed via directory listings of scripts/ and .github/workflows/ above); grep for 'injectFault' across the repo returns no matches.
Gap: No fault-drill harness exists for any of the 8 fault classes (feed-down, mock-pollution, 404-spike, index-drop, postback-anomaly, cost-spike, cron-no-op, db-restore). This is the stated verification vehicle for all of S6, and since T-OPS-1 through T-OPS-5 are all absent, there is nothing for it to verify even if it existed.
